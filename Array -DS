func reverseArray(a []int32) []int32 {
     
    var result []int32
    for index:= 0; index < len(a) ; index ++ {
       temp := (len(a)-1) - index
       result = append(result,a[temp])
    }
    return result

}
